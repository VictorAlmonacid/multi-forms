# multi-forms
Dynamic Form Builder with Validation

This project provides a dynamic form system using vanilla JavaScript. Users can add or remove input groups (forms) on the fly, with real-time and full-form validation based on custom HTML data attributes. The first form group is always present and cannot be removed. Includes support for string, number, boolean, and decimal validations.
